
    var a = 1, b = 1, c, d;
    c = ++a; alert(c);           // 2 - В префиксной форме инкрементирование производится сразу, а возврат — уже с обновленным значением.


    d = b++; alert(d);           // 1 - В постфиксной форме сначала происходит возвращение значения, а потом выполняется инкрементирование.

    c = (2+ ++a); alert(c);      // 5 - из предыдущей операции ++a a=2, здесь выполняется ++a - это 3 и плюс 2.

    d = (2+ b++); alert(d);      // 4 - из предыдущей операции b=2, в данном примере инкрементирование выполнится после присвоения, поэтому 2+2=4

    alert(a);                    // 3 - в результате вышепроизведенных операций

    alert(b);                    // 3 - для b выполнилось инкрементирование, теперь b=3.
